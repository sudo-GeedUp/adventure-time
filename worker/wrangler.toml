name = "adventure-time-ai-proxy"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Secrets are set out-of-band, never here (this file is committed):
#   npx wrangler secret put OPENAI_API_KEY
#   npx wrangler secret put REVENUECAT_API_KEY
[vars]
FIREBASE_PROJECT_ID = "thegoodadventuretime"
REVENUECAT_PROJECT_ID = "proj7bb7522b"
# Internal id of the "It's Adventure Time Pro" entitlement, not its lookup key —
# the active_entitlements endpoint only ever reports the internal one.
REVENUECAT_ENTITLEMENT_ID = "entl625ed50f2c"
